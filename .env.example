# The Kopala ICR 2026 backend API. Not built yet — every request against
# this base URL will fail until that backend exists, which is expected:
# the frontend here is fully wired for the real endpoints (see src/api/),
# but nothing to point it at exists yet. Set to the real deployed address
# once the backend is available.
VITE_API_BASE_URL=http://localhost:8004
